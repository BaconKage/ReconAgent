# Everything the demo needs. Note what is NOT here: no LLM SDK.
# The deterministic engine, all metrics, the exception list, the cash position
# and the Q&A run entirely on these, replaying committed reasoning traces.
pandas==2.3.3
streamlit==1.62.0
python-dotenv==1.2.1
pytest==9.1.1
