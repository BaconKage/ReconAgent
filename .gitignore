__pycache__/
*.pyc
.pytest_cache/
audit_trail/*.jsonl

# Batches built from a Razorpay recon report. The fixture and the sample bank
# export in data/razorpay_sample/ are tracked; what the adapter derives is not.
data/rzp/
.venv/
venv/

# Secrets. Every dotenv variant is ignored; only the template is tracked.
.env
.env.*
!.env.example

# Streamlit secrets. The deployed demo needs no key at all - it replays committed
# traces - but if one is ever set locally it must not be committed.
.streamlit/secrets.toml

# Personal working files, not part of the submission. Listed explicitly because
# `git add -A` has already swept something into this repo once.
VIDEO_SCRIPT.md
